[project]
name = "lab-03-scripting"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Luke Crater", email = "xbz7uc@virginia.edu" }
]
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
lab-03-scripting = "ghevents:main"

[tool.uv.build-backend]
module-name = "ghevents"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
